language: python
python:
  - "3.5"
  - "3.4"
  - "2.7"
  - "pypy"
before_install:
  - pip install pytest pytest-cov
  - pip install moto
  - pip install coveralls
install:
  - pip install .
script:
  - py.test --cov-report term-missing --cov acli
after_success:
  coveralls
